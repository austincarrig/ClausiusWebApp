pub enum Direction {
    X,
    Y
}

pub enum ValueType {
    Temperature,
    Pressure,
    SpecificVolume,
    InternalEnergy,
    Enthalpy,
    Entropy
}

pub enum ScaleType {
    Linear,
    Log
}

pub struct Axis;

impl Axis {
}